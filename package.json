{
  "name": "backend",
  "version": "1.0.0",
  "proxy": "http://localhost:5000",
  "license": "MIT",
  "scripts": {
    "start": "json-server --watch db.json --port 5000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
