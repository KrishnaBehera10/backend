{
  "homepage": "https://krishnabehera10.github.io/ecommerce-json-site",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
